// render-helpers/helpers/render-slot.js *本次暂未实现，用于实现插槽功能的渲染
import {warn} from "../../shared/utils.js";

export const renderSlot = (name, fallback, porps, bindObj) => {
    // const scopeSlotn = this.$scopedSlots(name);
    // TODO 暂不实现
    warn(`插槽功能暂未实现`);
};